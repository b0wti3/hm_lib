function (context, args) {
  // Provides the user an interace to create, edit, and even delete page content.
    // As a guest, they have no access to this editor. Instead they are prompted to login to the service.
    // As a member, a user can manage pages that they have created themselves. These pages are stored in the 'edit' or 'dev' database on the user 'ap'. They can then request content to be submitted to the service.
    // As a admin, a user can manage all member content, including the following ways:
      // - approving/denying submit requests
      // - editing / deleting content through service bots/superadmin interaction
      // - create and edit system notice information, as well as news content, and profile creation/editing
}